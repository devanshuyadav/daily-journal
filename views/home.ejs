<%- include("partials/header");-%>

    <h1>Home</h1>
    <p>
        <%= hsc%>
    </p>
    <!-- <% for(var i=0; i < posts.length; i++) { %>
        <h2>
            <%= posts[i].title %>
        </h2>
        <p>
            <%= posts[i].body %>
        </p>
        <% } %> -->

    <hr>

    <!-- using foreach instead of for loop -->
    <% posts.forEach(function(e){ %>
        <h2>
            <%=e.title%>
        </h2>
        <p>
            <%=e.body.substr(0,100) + ('...')%> <a href="/posts/<%=e.title%>">Read More</a>
        </p>
        <% }); %>
            <a href="/compose">
                <button type="button" class="btn btn-danger home_new_btn" btn-lg btn-block type="submit">
                    <i class="far fa-edit"></i>Write a New Post
                </button></a>
            <%- include("partials/footer");-%>